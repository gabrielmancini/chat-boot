{
  "name": "financial",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_DEBUG=chat node -r dotenv/config index.js",
    "debug": "node debug -r dotenv/config index.js",
    "dev": "npm run lt || npm start",
    "lt": "lt -p 3000 -s financialbot &",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootbot": "^1.0.14",
    "dotenv": "^5.0.0"
  },
  "devDependencies": {
    "localtunnel": "^1.8.3"
  }
}
